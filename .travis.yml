language: node_js
node_js:
- '6'
sudo: false
before_script:
- npm install -g grunt-cli
- npm install -g grunt-init
script:
- mkdir output
- cd output
- grunt-init ../
- ../travis/runner `dirname $(pwd)`
- npm install
- npm test
addons:
  apt:
    packages:
    - expect
